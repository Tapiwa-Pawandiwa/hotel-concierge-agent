# Duplicate of ../requirements.txt, required here specifically because
# `adk deploy cloud_run` looks for requirements.txt INSIDE the agent's own
# source folder (agent/concierge_agent/), not in the parent agent/
# directory where this project's convention has kept it since T002 --
# confirmed directly in google-adk's cli_deploy.py. Keep both files in
# sync manually until/unless the project's layout changes.
google-adk[extensions]==2.8.0
psycopg==3.3.4
psycopg-binary==3.3.4
pgvector==0.5.0
voyageai==0.5.0
python-dotenv==1.2.2
Faker==40.35.0
